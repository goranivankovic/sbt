import { useState } from 'react';

function App() {
  const [shifted, setShifted] = useState(false);

  const handleMouseEnter = () => setShifted(true);
  const handleMouseLeave = () => setShifted(false);

  return (
    <div style={{ padding: '50px' }}>
      {/* Prvi div - ostaje na mestu */}
      <div
        onMouseEnter={handleMouseEnter}
        onMouseLeave={handleMouseLeave}
        style={{
          width: '200px',
          height: '100px',
          backgroundColor: '#add8e6',
          textAlign: 'center',
          lineHeight: '100px',
          marginBottom: '20px',
          cursor: 'pointer',
        }}
      >
        Pređi mišem ovde
      </div>

      {/* Drugi div - pomera se udesno */}
      <div
        style={{
          width: '100px',
          height: '100px',
          backgroundColor: 'tomato',
          transform: shifted ? 'translateX(400px)' : 'translateX(0)',
          transition: 'transform 0.3s ease',
        }}
      >
        Ja idem 👉
      </div>
    </div>
  );
}

export default App;
